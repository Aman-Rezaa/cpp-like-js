const Deque = require('./src/deque');
const LinkedList = require('./src/linked_list');
const Vector = require('./src/vector');
const {cin,cout} = require('./src/cin_cout');
const Stack = require('./src/stack');
const {PriorityQueueMinOnTop , PriorityQueueMaxOnTop} = require('./src/priority_queues');
const Queue = require('./src/queue');
const Tree = require('./src/tree');


module.exports = {
    Deque,
    LinkedList,
    Vector,
    cin,
    cout,
    Stack,
    PriorityQueueMinOnTop,
    PriorityQueueMaxOnTop,
    Queue,
    Tree
  };

